machine:
  environment:
    COVERAGE: true
    NODE_ENV: test
    XUNIT: true
    UNIT_ONLY: true
  node:
    version: 4

general:
  artifacts:
    - lerna-debug.log
    - npm-debug.log

test:
  pre:
    - npm run depcheck
    - npm run grunt:circle -- build:
        parallel: true
  override:
    - npm run grunt:circle -- static-analysis test:
        parallel: true
