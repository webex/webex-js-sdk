machine:
  node:
    version: 4

dependencies:
  cache_directories:
    - node_modules
    - packages/*/node_modules
  override:
    - npm install
    - npm run bootstrap

test:
  override:
    - ./node_modules/.bin/grunt --gruntfile Gruntfile.circle.js static-analysis
