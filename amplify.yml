version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --immutable
    build:
      commands:
        - yarn run build:local
        - yarn run build:docs
  artifacts:
    baseDirectory: docs/
    files:
      - '**/*'
