version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --immutable
    build:
      commands:
        - yarn workspace sdk-portal run build
  artifacts:
    baseDirectory: docs-new/sdk-portal/build
    files:
      - '**/*'
