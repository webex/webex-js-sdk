import Package from './package';

export type {
  BuildConfig as PackageBuildConfig,
  Data as PackageData,
  TestBrowser as PackageTestBrowser,
  TestConfig as PackageTestConfig,
  TestRunner as PackageTestRunner,
} from './package.types';

export default Package;
