const CONFIG = {
  diff: true,
  bail: true,
  require: ['@babel/register', '@webex/env-config-legacy'],
  retries: 0,
  timeout: 30000,
};

const CONSTANTS = {
  CONFIG,
};

export default CONSTANTS;
