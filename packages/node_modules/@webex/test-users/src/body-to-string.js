/**
 * Converts a request body into a stringified string
 *
 * @export
 * @param {*} body
 * @returns {String}
 */
export default function bodyToString(body) {
  try {
    return JSON.stringify(body, null, 2);
  }
  catch (err) {
    return body;
  }
}
