/* eslint-env browser */
// Any references to 'ciscospark' should be removed once this becomes an actual plugin

import CiscoSpark from 'ciscospark';
import {registerPlugin} from '@ciscospark/spark-core';

import Meetings from './meetings';
import config from './config';

// TODO: Remove this when we are ready to merge into master and are in the regular samples directory
window.ciscospark = CiscoSpark;

registerPlugin('meetings', Meetings, {
  config
});

// export {default} from './meetings';
// export {default as config} from './config';
