import {assert} from '@webex/test-helper-chai';

describe('plugin-wdm', () => {
  describe('Device', () => {
    it('does not have any unit tests', () => {
      assert.isTrue(true);
    });
  });
});
