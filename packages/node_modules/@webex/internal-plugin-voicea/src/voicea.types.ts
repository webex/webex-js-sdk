/**
 * Type for payload
 */
class AnnouncementPayload {
  translation:{
    max_languages: number;
    allowed_languages: string[]
  };

  ASR: {
    spoken_languages: string[];
    };
}

/**
 * Type for CaptionLanguageResponse
 */
class CaptionLanguageResponse {
  requestId: string;

  statusCode: number;

  errorCode:number;

  message:string;
}

// eslint-disable-next-line import/prefer-default-export
export {AnnouncementPayload, CaptionLanguageResponse};
