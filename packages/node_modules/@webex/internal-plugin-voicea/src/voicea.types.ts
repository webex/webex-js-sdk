/**
 * Type for payload
 */
class VoiceaPayload {
  translation:{
    max_languages: number;
    allowed_languages: string[]
  };

  ASR: {
    spoken_languages: string[];
    };
}

// eslint-disable-next-line import/prefer-default-export
export {VoiceaPayload};
