addEventListener('fetch', (event) => {
  if (revent.request.url === "https://myspark.cisco.com/spark_session_check.json") {
    event.respondWith(async function() {
      return new Response(JSON.stringify({ping: 'pong'}), {headers: {'content-type': 'application/json'}})
    })
  }
})
