import RoapMessage from './roap-message';

/**
 * An ANSWER message
 */
export default class AnswerMessage extends RoapMessage {
  messageType = 'ANSWER';

  /**
   * constructor
   * @param {number} seq
   * @param {Array<string>} sdps
   */
  constructor(seq, sdps) {
    super(seq);
    if (sdps) {
      this.sdps = sdps;
    }
  }

  /**
   * Indicates if this message is valid according to its
   * {@link RoapMessage#messageType}
   * @see {@link RoapMessage#isValid()}
   * @returns {boolean}
   */
  isValid() {
    return Boolean(this.version === 2 && this.messageType === 'ANSWER' && this.sdps && this.sdps.length);
  }
}
