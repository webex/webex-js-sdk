import AmpCollection from 'ampersand-collection';
// import {defaults} from 'lodash';

import CallMempership from './call-membership';

const CallMemperships = AmpCollection.extend({
  model: CallMempership,

  mainIndex: `id`,

  indexes: [`personId`]

  // // parse(attrs, options) {
  // //   attrs = Array.isArray(attrs) ? attrs : [attrs];
  // //   return attrs
  // //     .filter((participant) => participant.type === `USER`)
  // //     .map((participant) => {
  // //       parseId:
  // //     })


  // //   console.log(attrs, options);
  // //   return Reflect.apply(AmpCollection.prototype.parse, this, [attrs, options]);
  // // },

  // set(attrs, options = {}) {
  //   defaults(options, {parse: true});

  //   return Reflect.apply(AmpCollection.prototype.set, this, [attrs, options]);
  // }
});

export default CallMemperships;
