/**
 * {@link Buffer} in NodeJS, {@link ArrayBuffer} in browsers
 * @typedef {(ArrayBuffer|Buffer)} BufferLike
 */

/**
 * {@link BufferLike} or (in browsers) {@link File} or {@link Blob}
 * @typedef {(BufferLike|File|Blob)} FileLike
 */

/**
 * {@link string} containing the url to a key or a {@link Key} instance
 * @typedef {(string|Key)} KeyLike
 */

/**
 * @typedef {Object} Key
 */

/**
 * @typedef {Object} KMSResourceObject
 * @property {string} uri
 * @property {Array<string>} keyUris
 * @property {Array<string>} authorizationUris
 */
