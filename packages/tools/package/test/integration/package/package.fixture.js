const packageConstructorConfig = {
  location: './example/location',
  name: '@example-scope/example-package',
  tag: 'example-tag',
};

const fixtures = {
  packageConstructorConfig,
};

module.exports = fixtures;
