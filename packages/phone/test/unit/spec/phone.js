/**!
 *
 * Copyright (c) 2015 Cisco Systems, Inc. See LICENSE file.
 */

'use strict';

var assert = require('@ciscospark/test-helper-chai').assert;
require('../..');

describe('phone', () => {
  it('has just enough of a test suite to not crash the test suite', () => {
    assert.isTrue(true);
  });
});
