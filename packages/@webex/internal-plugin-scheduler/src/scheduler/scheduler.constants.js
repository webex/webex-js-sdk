// Namespace to store this plugin under within `webex` or `webex.internal`.
const NAMESPACE = 'scheduler';

const SCHEDULER_REGISTERED = 'scheduler:registered';
const SCHEDULER_UNREGISTERED = 'scheduler:unregistered';

const CONSTANTS = {
  NAMESPACE,
  SCHEDULER_REGISTERED,
  SCHEDULER_UNREGISTERED,
};

export default CONSTANTS;
