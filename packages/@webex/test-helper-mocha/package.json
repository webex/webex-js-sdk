{
  "name": "@webex/test-helper-mocha",
  "version": "3.0.0-beta.32",
  "description": "Helpers for controlling where tests run",
  "license": "MIT",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/webex/webex-js-sdk.git",
    "directory": "packages/@webex/test-helper-mocha"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "bowser": "^2.11.0"
  },
  "scripts": {
    "types": "yarn run -T tsc --declaration true --declarationDir ./dist/types",
    "doc:api": "api-extractor run --local --verbose",
    "doc:md": "api-documenter markdown -o ./api/markdown -i ./api"
  }
}
